using UnityEngine;

public class TestScript : MonoBehaviour {
    public void Update() {
        // if (Input.GetKeyDown(KeyCode.Return))
        //     transform.forward = Quaternion.Euler(0, 90f, 0);
    }
}